package main //the word "main" is used to make an executable type of package as opposed ot other packages like helpers

import "fmt" //library package included in Go by default for format

func main() {
	fmt.Println("Hi there!!!!")
}
